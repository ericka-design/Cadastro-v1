import React from 'react';
import { render, screen } from '@testing-library/react';
import  {App}  from './App';  // Importação padrão

test('renders learn react link', () => {
  render(<App />);
  // Adicione aqui os testes desejados
});
